# Frontend

This folder is intended for frontend development. You can use frameworks like React, Vue.js, or Angular here. After building the frontend, place the static files in the `static/` directory for integration with Django.